<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="{{ asset('img/glider.svg') }}">
        {# <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>"> #}
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <header class="site-header">
        {# <header class="site-header" style="background-image: url({{ asset('img/background.jpg') }})"> #}
            <span class="site-title">My Symfony Site</span>
        </header>
        <nav>
            <ul>
                <li><a href="{{ path('home') }}">Hem</a></li>
                <li><a href="{{ path('about') }}">Om</a></li>
                <li><a href="{{ path('report') }}">Redovisning</a></li>
                <li><a href="{{ path('lucky') }}">Tur eller otur</a></li>
                <li><a href="{{ path('api') }}">API/JSON</a></li>
                <li><a href="{{ path('card_start') }}">Kort</a></li>
                <li><a href="{{ path('game_home') }}">Kortspel</a></li>
            </ul>
        </nav>
        <main>{% block body %}{% endblock %}</main>
        <footer>{% block footer %}Skapad av Gabriel Rahm | gara20{% endblock %}</footer>
    </body>
</html>
